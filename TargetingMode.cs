public enum TargetingMode
{
    Nearest,        // Le plus proche (Classique)
    HighestDensity, // Le groupe le plus compact (Pour les explosions)
    Random          // Au hasard (Pour le chaos ou tirs de suppression)
}